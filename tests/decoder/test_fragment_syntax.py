import pytest


@pytest.mark.xfail
def test_todo():
    # The tests for the fragment syntax will be substantially easier to write
    # once bitstream generation routines have been written and so development
    # of these tests will be suspended until then.
    assert False
